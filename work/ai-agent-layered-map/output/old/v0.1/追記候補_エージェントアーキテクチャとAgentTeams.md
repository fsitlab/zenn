# 追記候補：エージェントアーキテクチャの層構造とClaude Code Agent Teams

> **元記事の対応章**: 第8章（SubAgents）、第9章（Agent Teams）の改訂・拡充
> **追記 or 変更**: 変更（両章を4層モデルで再整理）
> **理由**: A2A・SubAgents・Agent Teamsの違いが「通信プロトコルの層の違い」として整理されていない。Agent Teamsの内部構造（②層ツール群＋③層Skill連携）が未記載。

---

## 第8章補足　A2A・SubAgents・Agent Teamsの層の違い

**一言**: 3つは「エージェントが別のエージェントを呼ぶ」という点では同じ。違いは通信路がどの層にあるか、そして呼び出し先を直接知っているかどうか。

---

### 本質はクライアント・サーバー

SubAgentsとA2Aは構造的に同じ。

```
クライアント（呼び出す側）
  └─ サーバー（呼び出される側）を呼ぶ
        └─ サーバー内部で①②③が走る
        └─ 結果を返す
```

違いは**通信路の層だけ**。

| | 通信路 | 層 | 呼び出し先を知っているか |
|--|--|--|--|
| SubAgents | tool_use / tool_result | ①層 | 知っている（親が子を直接指定） |
| A2A | HTTP / JSON-RPC 2.0 | ④層間 | Agent Cardで発見して知る |
| 従来のWeb API呼び出し | HTTP / REST | ④層間 | 知っている |

**SubAgentsはLLMの視点では「ツールを呼んだら結果が返ってきた」だけ**。内部で別セッションが走っていることを意識しない。

---

### A2Aの構造

各エージェントは`/.well-known/agent.json`（Agent Card）を公開し、互いを発見する。

```json
{
  "name": "DataAnalysisAgent",
  "url": "https://agent-b.example.com",
  "skills": [
    {"name": "analyze_csv", "description": "CSVを統計分析する"}
  ]
}
```

```
④層エージェントA（オーケストレーター）
│
│  JSON-RPC 2.0 over HTTP/SSE ← ④層間の通信
│
④層エージェントB（ワーカー）
  └─ 受け取ったタスクをBの①②③で処理して返す
```

**①②③を経由するのではなく、B内部で①②③が走る**。AはBの内部実装を知らない。

**応答の受け取りは2モード**:

```
同期モード:  A → POST → B → レスポンス返却（1往復）
SSEモード:  A → POST → B → event: 進捗 → event: 完了（継続受信）
```

---

### オーケストレーターの位置による分類

```
パターン1: LLMがオーケストレーター
  ①層でtool_useを返す
  → ②層がA2A / SubAgent呼び出しを実行
  → 結果がtool_resultとして①層に戻る

パターン2: プログラムがオーケストレーター
  ④層のワークフローエンジン（LangGraph等）が
  直接A2AでエージェントBを呼ぶ
  LLMは関与しない
```

---

## 第9章改訂　Claude Code Agent Teams：新プロトコルではなく②層ツールの工夫

**一言**: Agent TeamsはA2Aのような新通信プロトコルではない。**②層のツール群（TaskCreate/SendMessage等）を④層のClaude Codeが利用する設計**であり、既存の4層構造の中に完全に収まる。

> 📘 [Claude Code Agent Teams公式ドキュメント](https://code.claude.com/docs/en/agent-teams)
> ※experimental機能。`settings.json`に`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS: "1"`で有効化。

---

### 4層での位置づけ

```
④層: Agent Teams機能全体（Claude Codeのアプリ機能）
      リード＋テイメイトの複数セッションが並列実行

②層: タスクJSONとInboxJSONの管理ランタイム
      ファイルロック・依存関係の自動解除
      TaskCreate/TaskUpdate/TaskList/SendMessageの実行

①層: 各ツール（TaskCreate等）のtool_use定義と呼び出し
      各テイメイト自身のLLMリクエスト/レスポンス

③層: 各テイメイトのCLAUDE.md・Skill（独立してロード）
      ※リードの会話履歴は引き継がない
```

---

### 内部タスクボードの構造

②層のランタイムが管理するJSONファイル群。

```
~/.claude/tasks/{team-name}/
├── 1.json   ← TaskCreateで生成
├── 2.json
└── 3.json

~/.claude/teams/{team-name}/
├── config.json        ← チーム構成・メンバー一覧
└── inboxes/
    ├── team-lead.json ← SendMessageで届く
    ├── worker-1.json
    └── worker-2.json
```

**協調チャンネルは2つ**:

```
チャンネル1: タスクリスト（共有黒板）
  TaskCreate  → タスクを作る
  TaskUpdate  → 状態変更（TODO→進行中→完了）
  TaskList    → 一覧確認
  ファイルロックで二重クレーム防止

チャンネル2: ダイレクトメッセージ（Inbox）
  SendMessage → 特定エージェントに直接送る
  broadcast   → 全員に送る
  非同期。相手のinbox JSONに蓄積される
```

---

### 内部タスクボードは動的に変化する

最初に作って終わりではない。タスク実行中も状態が変わり続ける。

```
リードが最初に作る
  TaskCreate → task1「リサーチ」
  TaskCreate → task2「実装」（task1に依存・ブロック状態）
  TaskCreate → task3「テスト」（task2に依存・ブロック状態）
      ↓
worker-1がtask1を拾う → TaskUpdate「進行中」
worker-1が完了 → TaskUpdate「完了」
  → task2のブロックが自動解除（②層のランタイムが担当）
      ↓
リードがリサーチ結果を見て追加判断
  TaskCreate → task4「追加調査」  ← 途中追加
      ↓
テイメイト自身が新タスクを生やすこともある
  TaskCreate → task5「リファクタリング」  ← エージェント発
```

**タスクボードは生きている状態機械**。依存関係の解除は②層ランタイムが自動で行い、テイメイトはポーリング不要。

---

### SubAgentsとAgent Teamsの本質的な違い

Agent TeamsがSubAgentsより優れている点は並列実行だけではない。

```
SubAgents
  親 → tool_use → 子（1対1）
  子が動いている間、親は待っている
  テイメイト同士は話せない
  人間の割り込み = セッションを止めるしかない

Agent Teams
  リード → タスクボード（②層）← テイメイト全員が参照
  テイメイト同士がSendMessageで直接話せる
  人間はいつでも参照・介入できる
```

---

## 第9章追加　人間の介入：③層Skillと外部Markdownの連携

**一言**: ②層の内部タスクボードが「何をやるか」を管理する一方、③層のSkill経由で参照される外部Markdownファイルが「なぜやるか・どう判断するか」を補完する。この二層構造が、Agent Teamsへの自然な人間介入を可能にする。

---

### 内部タスクボードと外部Markdownの役割分担

| | 内部タスクボード | 外部Markdownファイル |
|--|--|--|
| 管理主体 | ②層ランタイム | ③層（Skill/CLAUDE.mdから参照） |
| 内容 | 何をやるか・今どこか・依存関係 | なぜやるか・方針・判断軸・中間成果 |
| 変化のタイミング | タスク実行のたびに動的変化 | 人間・エージェントが書いたタイミング |
| 主な書き手 | エージェント（TaskCreate/TaskUpdate等の専用ツール） | **人間もエージェントも**（Read/Writeツールで自由に編集） |
| 読む主体 | エージェントのみ | 人間もエージェントも |

---

### 接続の仕組み

外部MarkdownとAgent Teamsが連携する理由は、③層の指示にある。

```
CLAUDE.md または Skill に書く（③層）:
  "タスク開始前に /project/whiteboard.md を必ず読むこと"
  "方針変更は /project/whiteboard.md に追記すること"
        ↓
全テイメイトがセッション開始時に③層をロード
        ↓
whiteboard.md が全テイメイトの共有コンテキストになる
```

---

### 人間の割り込みの3段階

```
強制停止（SubAgents時代）
  セッションを止めるしかない。作業は失われる。

リアルタイム割り込み（Agent Teams: SendMessage）
  進行中のテイメイトに直接メッセージを送る。
  即時に届くが、テイメイトの応答タイミングは不定。

非同期割り込み（外部Markdownファイル）
  whiteboard.md を人間またはエージェントが書き換える。
  次にタスクを拾ったエージェントが読むタイミングで反映。
  エージェントの作業を中断せず、キリのいい
  タイミングで方針・中間成果が伝わる。
```

**whiteboard.mdへの書き込み主体は人間だけではない**。エージェントも通常のWriteツール（tool_use）でwhiteboard.mdに書き込める。内部タスクボードの専用ツール（TaskCreate等）とは無関係に、汎用のRead/Writeツールで操作できる通常のファイルであることが重要。

これにより「エージェントが中間成果をwhiteboard.mdに書き、人間がそれを読んで方針を追記し、別のエージェントがその方針を読んで動く」という**人間とエージェントが同じファイルに交互に書くループ**が成立する。

---

### Agent Teamsが変えたもの

> SubAgentsとA2Aの違いは、クライアント・サーバー通信のプロトコルが①層（tool_use）か④層（HTTP）かの差でしかない。どちらも内部で①②③が走る構造は同一。
>
> Agent Teamsはプロトコルの話ではなく、**②層ツール群の工夫によってテイメイト間の直接通信と人間介入の設計を実現したもの**。新しい層を追加したのではなく、既存4層の中で協調の質を上げた。

---

### 参考リンク
- 📘 [Claude Code Agent Teams 公式ドキュメント](https://code.claude.com/docs/en/agent-teams)
- 📘 [Claude Code Sub-agents 公式ドキュメント](https://code.claude.com/docs/en/sub-agents)
- 📘 [Google A2A プロトコル仕様](https://google.github.io/A2A/)
- 📘 [Amazon Bedrock AgentCore A2A対応](https://aws.amazon.com/bedrock/agentcore/)
- 🔗 [From Tasks to Swarms: Agent Teams in Claude Code (alexop.dev)](https://alexop.dev/posts/from-tasks-to-swarms-agent-teams-in-claude-code/) — 内部アーキテクチャの詳細解説
- 🔗 [Orchestrate teams of Claude Code sessions (公式ドキュメント)](https://code.claude.com/docs/en/agent-teams)

---

*作成日: 2026-02-21*
*対象: 骨子バージョン 2026-02-21 rev2 への差分*
*この会話で明確化した概念:*
*- A2A・SubAgents・Agent Teamsの通信路の層の違い*
*- Agent Teamsは新プロトコルではなく②層ツール群の設計*
*- 内部タスクボード（動的状態機械）vs 外部Markdown（人間の意図）*
*- 3段階の人間介入（強制停止・リアルタイム・非同期）*
